language: node_js
node_js:
  - "node"
notifications:
  email:
    on_failure: change
    on_success: never
install:
  - yarn install
script:
  - yarn build
  - jest --coverage --coverageReporters=text-lcov | coveralls
deploy:
  local_dir: 'lib/'
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN 
  keep-history: true
  target-branch: build
  on:
    branch: master